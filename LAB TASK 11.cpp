#include <iostream>
using namespace std;

int main() {
    int currentpopulation;   
    int bithpermonth;           
    cout << "Enter current world population: ";
    cin >> currentpopulation;

    cout << "Enter number of people born each month: ";
    cin >> birthpermonth;
    int months = 30 * 12;
    int future_population = population + (n * months);
    cout << "Population after 3 decades: " << future_population << endl;

    return 0;
}